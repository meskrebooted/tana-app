{
  "name": "tana-coworking-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "deploy": "npm run build:frontend && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  }
}
